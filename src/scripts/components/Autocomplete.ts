import * as React from 'react';

export default class Autocomplete extends React.Component {
    constructor() {
        this.handleClick = this.handleClick.bind(this);
    }

    public handleClick(event: Object): string {
        return '';
    }
}